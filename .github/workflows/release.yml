name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build-deb:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    container:
      image: debian:trixie
    outputs:
      deb_filename: ${{ steps.get_deb.outputs.filename }}
    steps:
      - uses: actions/checkout@v4

      - name: Install build dependencies
        run: |
          set -euo pipefail
          apt-get update
          apt-get install -y --no-install-recommends \
            ca-certificates \
            build-essential \
            cargo \
            rustc \
            dpkg-dev \
            debhelper \
            file \
            xz-utils

      - name: Build Debian package
        run: |
          set -euo pipefail
          dpkg-buildpackage -uc -us -b

      - name: Move debs to current directory
        run: |
          set -euo pipefail
          mv ../lyxcat_*.deb .

      - name: Get deb filename
        id: get_deb
        run: |
          echo "filename=$(ls *.deb)" >> $GITHUB_OUTPUT

      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref_name }}
          release_name: Release ${{ github.ref_name }}
          draft: false
          prerelease: false

      - name: Upload Release Asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ${{ steps.get_deb.outputs.filename }}
          asset_name: ${{ steps.get_deb.outputs.filename }}
          asset_content_type: application/vnd.debian.binary-package
